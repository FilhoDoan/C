#include <stdio.h>
#include <stdlib.h>


int main(){
/* 
Aloca uma quantidade de bytes
   Retorna ponteiro da memoria alocada 
   Retorna null caso de erro
   Retorna null ou um ponteiro em caso da quantidade ser zero
   A memoria não é inicializada 

*/

int *p = malloc(sizeof(int)); // 1 inteiro
char *nome = malloc(sizeof(char)*50); // string 50 posições
float *f = mallolc(sizeof(float)*10); // vetor float -10 posições





}